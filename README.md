# CloudSentinel
During a cloud security audit, I realized I was copy-pasting the same checklist across every engagement. So I automated it. CloudSentinel is a Python-based scanner that enumerates misconfigurations across core AWS services and generates a structured report with remediation steps — so you spend less time finding issues and more time fixing them.
